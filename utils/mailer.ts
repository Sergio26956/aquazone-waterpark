export async function sendContactForm(data: {
  nombre: string
  email: string
  mensaje: string
}) {
  // Simulación de envío (reemplazar por integración real si es necesario)
  console.log('Enviando mensaje de contacto:', data)
  return true
}
