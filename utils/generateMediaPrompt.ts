export function generateMediaPrompt(eventType: string, style: string) {
  return `Diseña un visual (imagen/video) promocional para '${eventType}' en estilo '${style}', enfocado en tecnología, agua y diversión.`;
}
