/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['your-domain.com'], // Cambiar si usas imágenes externas
  },
}

module.exports = nextConfig
